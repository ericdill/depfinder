Building the docs
-----------------

You need:

- conda install sphinx numpydoc
- conda install -c bokeh sphinx-rtd-theme

then inside the ``depfinder/doc`` folder, type ``make html``. The docs will
then be in ``depfinder.doc/_build/html/``